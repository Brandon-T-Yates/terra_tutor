import 'package:flutter/material.dart';

class AppColors {
  // Background color
  static const Color primaryColor = Color.fromARGB(255, 226, 217, 199);

  // Widget tile color
  static const Color uiTile = Color.fromARGB(255, 251, 241, 108);

  // Top and bottom Nav bar color
  static const Color navBar = Color.fromARGB(255, 194, 209, 97);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color.fromARGB(155, 255, 255, 255); // Transparency will be ajusted upon icon tsting

  // Menu icon colors
  static const Color menuIconClicked = Color.fromARGB(255, 0, 0, 0);
  static const Color menuIconUnclicked = Color.fromARGB(255, 127, 127, 127);

  // Font color
  static const Color fontColor = Color.fromARGB(255, 0, 0, 0);

  // Delete button color
  static const Color deleteButton = Color.fromARGB(127, 255, 0, 0);
}

class AppColorsAlternativeTropicalGarden {
  // Background color
  static const Color primaryColor = Color.fromARGB(255, 226, 217, 199);

  // Widget tile color
  static const Color uiTile = Color(0XFF7D9E96);

  // Top and bottom Nav bar color
  static const Color navBar = Color(0xFFADC2AF);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color(0XFF7D9E96);
  // Menu icon colors
  static const Color menuIconClicked = Color(0XFF000000);
  static const Color menuIconUnclicked = Color.fromARGB(255, 127, 127, 127);

  // Font color
  static const Color fontColor = Color(0XFF000000);
  static const Color whiteFont = Color(0XFFFFFFFF);

  // Delete button color
  static const Color deleteButton = Color.fromARGB(127, 255, 0, 0);
}

class AppColorsAlternativeWoodlandForest {
  // Background color
  static const Color primaryColor = Color(0xFF3E563D);
  static const Color scaffoldBackgroundColor = Color(0xFFF5ECE9);

  // Widget tile color
  static const Color uiTile = Color(0xFF6B8F71);

  // Top and bottom Nav bar color
  static const Color navBar = Color(0xFF3E563D);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color(0xFF6B8F71);

  // Menu icon colors
  static const Color menuIconClicked = Color(0xFFFFFFFF);
  static const Color menuIconUnclicked = Color(0xFFB5C6A6);

  // Font color
  static const Color fontColor = Color(0xFF000000);
  static const Color whiteFont = Color(0XFFFFFFFF);
  // Delete button color
  static const Color deleteButton = Color(0xFFFF6961);
}

class AppColorsAlternativeHighDesert {
  // Background color
  static const Color primaryColor = Color(0xFFEDC9AF);
  static const Color scaffoldBackgroundColor = Color(0xFFFFF5EC);
  // Widget tile color
  static const Color uiTile = Color(0xFFD4A373);

  // Top and bottom Nav bar color
  static const Color navBar = Color(0xFFEDC9AF);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color(0xFFD4A373);

  // Menu icon colors
  static const Color menuIconClicked = Color(0xFF000000);
  static const Color menuIconUnclicked = Color(0xFF8B4513);

  // Font color
  static const Color fontColor = Color(0xFF000000);

  // Delete button color
  static const Color deleteButton = Color(0xFFFF4500);
}

class AppColorsAlternativeRedwoodForest {
  // Background color
  static const Color primaryColor = Color(0xFF4B2C20);

  static const Color scaffoldBackgroundColor = Color(0xFFF5ECE9);

  // Widget tile color
  static const Color uiTile = Color(0xFF8B5A2B);

  // Top and bottom Nav bar color
  static const Color navBar = Color(0xFF4B2C20);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color(0xFF8B5A2B);

  // Menu icon colors
  static const Color menuIconClicked = Color(0xFFFFFFFF);
  static const Color menuIconUnclicked = Color(0xFFD2B48C);

  // Font color
  static const Color fontColor = Color(0xFFFFFFFF);

  // Delete button color
  static const Color deleteButton = Color(0xFFB22222);
}

class AppColorsAlternativeArcticGarden {
  // primary color

  static const Color primaryColor = Color(0xFFE0F7FA);
  //Background color
  static const Color scaffoldBackgroundColor = Color(0xFFE5EFE7);
  // Widget tile color
  static const Color uiTile = Color(0xFFB2EBF2);

  // Top and bottom Nav bar color
  static const Color navBar = Color(0xFFE0F7FA);

  // Menu icon highlight box color
  static const Color menuIconHighLight = Color(0xFFB2EBF2);

  // Menu icon colors
  static const Color menuIconClicked = Color(0xFF00796B);
  static const Color menuIconUnclicked = Color(0xFF004D40);

  // Font color
  static const Color fontColor = Color(0xFF000000);
  static const Color whiteFont = Color(0XFFFFFFFF);
  // Delete button color
  static const Color deleteButton = Color(0xFFFF1744);
}
